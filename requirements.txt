pandas>=2.0
numpy>=1.24
pyarrow>=14.0
scipy>=1.10
scikit-learn>=1.3
pytest>=7.4
lightgbm>=4.0   # A2 Q2 re-ranker (LGBMRanker, lambdarank) -- CPU-only, doesn't itself need torch
torch>=2.0      # A2 Q3 NRMS comparison (scripts/train_nrms.py, retrieval/nrms.py) -- CPU works,
                # GPU strongly recommended for a full (non-subsampled) training run; see
                # scripts/train_nrms.sbatch for a SLURM/cluster job template.

# Optional: only needed for --semantic_backend sbert (scripts/build_indices.py).
# Not installed by default -- sentence-transformers pulls in its own torch (~1GB)
# if the line above hasn't already installed one.
sentence-transformers>=2.7
faiss-cpu>=1.8
